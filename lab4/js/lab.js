// Shuffle and deal cards
  //Leave rest of cards in middle of playing area
    // Evaluate possibilities (which card user wants)
    // Ask about certain cards (accept user input)
      // Validate user move - If they do have card, take it
      // If they don’t, draw from middle pile
    // Choose computer move
  // Test for win condition
